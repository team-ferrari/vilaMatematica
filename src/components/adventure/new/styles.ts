import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    container: {
        marginTop: -40,
        paddingHorizontal: 20,
    },
    actions: {
        paddingVertical: 30,
    },
});
