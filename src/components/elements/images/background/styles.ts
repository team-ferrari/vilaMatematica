import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    picture: {
        position: 'absolute',
        right: 0,
        width: 170,
        height: 150,
        resizeMode: 'contain',
    },
});
