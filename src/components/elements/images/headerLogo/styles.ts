import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    picture: {
        display: 'flex',
        resizeMode: 'contain',
        width: 80,
        height: 50,
    },
});
