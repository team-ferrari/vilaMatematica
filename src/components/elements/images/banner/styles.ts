import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    picture: {
        display: 'flex',
        resizeMode: 'contain',
        width: '100%',
        height: 200,
    },
});
