import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    container: {
    },
    action: {
        flex: 1,
        paddingHorizontal: 5,
        paddingVertical: 5,
    },
    box: {
        borderRadius: 10,
        paddingVertical: 10,
    },
});
