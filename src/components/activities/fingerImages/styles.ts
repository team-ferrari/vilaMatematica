import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    container: {
        flexDirection: 'row',
        paddingVertical: 10,
    },
    image: {
        flex: 1,
        aspectRatio: 1,
    },
});
